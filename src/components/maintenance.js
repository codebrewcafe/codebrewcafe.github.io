import React from "react"
import Layout from "../components/layout"

const Maintenance = () => (
  <>
    <h1>We'll Be Back</h1>
    <p>We're brewing a special blend at the moment, so check back later.</p>
  </>
)

export default Maintenance